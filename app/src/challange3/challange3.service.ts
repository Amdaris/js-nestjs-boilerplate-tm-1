import { Injectable } from '@nestjs/common';

@Injectable()
export class SupermarketQueueService {
  getResult(customers: number[], selfCheckoutPoints: number) {
    // Write the code for Challenge 3 here
    return -1;
  }
}
